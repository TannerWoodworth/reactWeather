var React = require('react');

var About = (props) => {
	return (
		<div>
			<h1 className="page-title text-center">About</h1>
			<p>This is a basic weather application built on React using the OpenWeather API.</p>
		</div>
		)
};

module.exports = About;